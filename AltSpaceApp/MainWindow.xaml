<Window x:Class="AltSpaceApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AltSpaceApp"
        mc:Ignorable="d"
        Title="AltSpace Search" Height="400" Width="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="False" ShowInTaskbar="False" WindowStartupLocation="CenterScreen"
        KeyDown="Window_KeyDown">
    <Border Background="#F0F0F0" CornerRadius="10" BorderBrush="#CCCCCC" BorderThickness="1">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="SearchBox" Grid.Row="0" Margin="0,0,0,10" FontSize="16" Padding="5" />
            <ListBox x:Name="ResultsList" Grid.Row="1" Background="White" BorderThickness="0" DisplayMemberPath="Name"/>
        </Grid>
    </Border>
</Window>
